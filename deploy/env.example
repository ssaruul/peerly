# Copy to /etc/peerly/env (mode 600, owned by root). install.sh does this for you.
#
# The admin key is needed once to create a group, and to recover a group whose
# owner has vanished. Anything long and random works: openssl rand -hex 24
PEERLY_ADMIN_KEY=replace-me-with-a-long-random-value
